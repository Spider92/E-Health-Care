@model IEnumerable<E_Health.Models.Information.Blocked_Medicine>

<div>
    @foreach (var item in Model)
    {
        <fieldset>

            @Html.ActionLink(@item.Title, "View_Blocked_Medicine", "Patient", new { id = item.MedicineId},null)<br>

        </fieldset>

    }
</div>




